<nav class="flex justify-between items-center bg-yellow is-blue text-xl px-4 py-2">
  <a href="/">
    <i class="fa-brands fa-trello"></i>
    Trello
  </a>
  <div>
    <% if user_signed_in? %>
      <span class="mr-2 nav-btn"><%= current_user.email %></span>
      <%= link_to "登出", destroy_user_session_path, method: :delete, class:'nav-btn' %>
    <% else %>
      <%= link_to '註冊', new_user_registration_path, class:'nav-btn' %>
      <%= link_to '登入', new_user_session_path, class:'nav-btn' %>
    <% end %>
  </div>
</nav>