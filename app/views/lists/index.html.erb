<div class="mt-2 px-1 flex">
  <% @lists.each do |list| %>
    <div class="is-light-yellow mx-2 w-64 rounded">
      <h2 class="px-3 py-1 font-bold is-blue"><%= list.name %></h2>
    </div>
  <% end %>
</div>
