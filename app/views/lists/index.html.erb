<div id="board" class="mt-2 px-1" data-lists="<%= @lists.to_json(include: :cards) %>">
  <Draggable class="flex" v-model="lists" @change="listMoved">
    <List v-for="list in lists" :list="list" :key="list.id"></List>
  </Draggable>
</div>
